#include <stdio.h>
main()
{
  char ascii ;
  int ascii_n ;

  printf("Saisir la valeur ASCII du caractère à afficher : ") ;
  scanf("%d), &ascii_n                                        ;
  ascii = ascii_n                                             ;
  printf("%c", ascii)                                         ;
}
